apiVersion: 1

providers:
  - name: paranoid-dashboards
    folder: Paranoid Monitoring
    type: file
    updateIntervalSeconds: 30
    options:
      # Path inside Grafana container
      path: /app/monitoring/dashboards
      # foldersFromFilesStructure: true


